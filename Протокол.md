# Протокол GPS-трекера (JT/T808)

## Устройство

| Параметр              | Значение                    |
| --------------------- | --------------------------- |
| ID терминала          | `19176726768`               |
| ID в логах прокси     | `9176726768` (без ведущей «1») |
| Протокол              | JT/T808                     |
| Транспорт             | TCP                         |
| Интервал телеметрии   | ~20 с (после начального всплеска) |
| Сервер производителя  | `27.aika168.com:8185`       |

Полный справочник типов пакетов — в файле [`Справочник-пакетов.md`](Справочник-пакетов.md).

---

## Последовательность подключения

### Наблюдаемая сессия (06.06.2026 11:02:19, клиент 2.73.108.168)

После установки TCP-соединения устройство **не отправляет 0x0100 (регистрацию)**. Сразу идёт **0x0102 (аутентификация)**; платформа отвечает **0x8001**, а не **0x8100**.

```text
0x0102  Аутентификация
  ↓
0x8001  Общий ответ
  ↓
0x0200  Телеметрия (всплеск: несколько пакетов за ~1 с)
  ↓
0x8001  Общий ответ (на каждый 0x0200)
  ↓
0x0002  Пульс (heartbeat)
  ↓
0x8001  Общий ответ (на 0x0002)
  ↓
0x0200  Телеметрия (~6 с, затем ~20 с)
  ↓
0x8001  Общий ответ
```

Хронология из захвата:

| Время    | Направление      | ID сообщения | Serial | Примечание                              |
| -------- | ---------------- | ------------ | ------ | --------------------------------------- |
| 11:02:19 | устройство → сервер | 0x0102    | 0x0144 | Тело = terminal ID (BCD)                |
| 11:02:20 | сервер → устройство | 0x8001    | 0x0199 | ACK на 0x0102, result = 0               |
| 11:02:20 | устройство → сервер | 0x0200    | 0x0142 | Телеметрия, тело 52 байта               |
| 11:02:20 | устройство → сервер | 0x0200    | 0x0143 | Телеметрия, тело 52 байта               |
| 11:02:20 | сервер → устройство | 0x8001    | 0x02C9 | ACK на 0x0142                           |
| 11:02:20 | устройство → сервер | 0x0200    | 0x0145 | Телеметрия, тело 84 байта + ICCID       |
| 11:02:20 | сервер → устройство | 0x8001    | 0x0399 | ACK на 0x0143                           |
| 11:02:21 | устройство → сервер | 0x0002    | 0x0146 | Пульс, пустое тело                      |
| 11:02:21 | сервер → устройство | 0x8001    | 0x0478 | ACK на 0x0145                           |
| 11:02:21 | сервер → устройство | 0x8001    | 0x056D | ACK на 0x0146                           |
| 11:02:27 | устройство → сервер | 0x0200    | 0x0147 | Телеметрия, тело 52 байта               |
| 11:02:27 | сервер → устройство | 0x8001    | 0x1148 | ACK на 0x0147                           |

### Стандартная последовательность JT/T808 (не наблюдалась)

По стандарту новое устройство может сначала отправить **0x0100** и получить **0x8100** с токеном аутентификации. На сервере `27.aika168.com:8185` для трекера `19176726768` этот шаг **не зафиксирован** — устройство использует свой terminal ID как код авторизации и пропускает регистрацию.

---

## Структура кадра

Все сообщения обрамляются байтом `0x7E`.

```text
7E
ID сообщения      2 байта
Свойства          2 байта   (биты 0–9: длина тела; бит 13: субпакет)
ID терминала      6 байт    (BCD)
Serial сообщения  2 байта
Тело              N байт
Контрольная сумма 1 байт    (XOR от ID сообщения до конца тела)
7E
```

Пример (телеметрия, тело 52 байта):

```hex
7E 02 00 00 34 01 91 76 72 67 68 01 42 ... F5 7E
```

### Поле «Свойства» — примеры из захвата

| Сообщение | Свойства | Длина тела |
| --------- | -------- | ---------- |
| 0x0102    | 0x0007   | 7          |
| 0x8001    | 0x0005   | 5          |
| 0x0002    | 0x0000   | 0          |
| 0x0200    | 0x0034   | 52         |
| 0x0200    | 0x0054   | 84         |

### Экранирование (byte stuffing)

Если в теле встречаются байты `0x7E` или `0x7D`, они экранируются:

| На проводе   | После разэкранирования |
| ------------ | ---------------------- |
| `7D 02`      | `7E`                   |
| `7D 01`      | `7D`                   |

В захваченной сессии экранированных байтов не было.

---

## ID терминала

Кодируется в BCD, 6 байт.

```hex
01 91 76 72 67 68
```

Расшифровка:

```text
19176726768
```

В логах прокси отображается как **9176726768** — без ведущей «1» у 11-значного номера.

---

## Типы сообщений

### 0x0100 — Регистрация

Стандартное первичное сообщение JT/T808.

**Не наблюдалось** на `27.aika168.com:8185`. Устройство могло быть зарегистрировано ранее, либо платформа принимает вход только по аутентификации.

При наличии сервер отвечает **0x8100**.

---

### 0x8100 — Ответ на регистрацию

Стандартный ответ на **0x0100**.

**Не наблюдался** в захвате. Формат токена аутентификации для этого сервера не подтверждён.

---

### 0x0102 — Аутентификация

Первое сообщение после TCP-подключения в наблюдаемой сессии.

```hex
7E 01 02 00 07 01 91 76 72 67 68 01 44 01 91 76 72 67 68 01 40 7E
```

Разбор:

```text
ID сообщения:   0x0102
Свойства:       0x0007 (тело 7 байт)
ID терминала:   19176726768
Serial:         0x0144
Тело:           01 91 76 72 67 68   (terminal ID в BCD — код авторизации)
Checksum:       0x40
```

Сервер отвечает **0x8001** (не 0x8100):

```hex
7E 80 01 00 05 01 91 76 72 67 68 01 99 01 44 01 02 00 C1 7E
```

Смысл: ACK serial **0x0144**, исходное сообщение **0x0102**, result **0** (успех).

---

### 0x0002 — Пульс (heartbeat)

Keepalive с пустым телом.

```hex
7E 00 02 00 00 01 91 76 72 67 68 01 46 DE 7E
```

Сервер отвечает **0x8001** (ACK serial **0x0146**, сообщение **0x0002**, result **0**).

В захвате пульс отправлялся **после** начального всплеска телеметрии, а не до первого **0x0200**.

---

### 0x0200 — Телеметрия (отчёт о местоположении)

Два размера тела:

| Свойства | Кадр    | Тело | Назначение в захвате              |
| -------- | ------- | ---- | --------------------------------- |
| 0x0034   | 68 байт | 52   | Обычное обновление координат      |
| 0x0054   | 100 байт| 84   | Телеметрия + ICCID (TLV 0xCC)     |

Пример (тело 52 байта):

```hex
7E 02 00 00 34 01 91 76 72 67 68 01 42
     00 00 00 00          ; флаги тревоги
     00 0C 00 02          ; флаги статуса
     02 93 A7 2D          ; широта
     04 94 DC 47          ; долгота
     03 4A                ; высота
     00 12                ; скорость
     01 07                ; направление
     26 06 06 17 01 48    ; время (BCD)
     ...                  ; дополнительные TLV
     F5 7E
```

#### Фиксированная часть тела (28 байт)

| Поле            | Размер      | Пример из захвата             |
| --------------- | ----------- | ----------------------------- |
| Флаги тревоги   | 4 байта     | `00 00 00 00`                 |
| Флаги статуса   | 4 байта     | `00 0C 00 02`                 |
| Широта          | 4 байта     | `02 93 A7 2D` → 43.231021°    |
| Долгота         | 4 байта     | `04 94 DC 47` → 76.864583°    |
| Высота          | 2 байта     | `03 4A` → 842 м               |
| Скорость        | 2 байта     | `00 12` → 1.8 км/ч (×0.1)     |
| Направление     | 2 байта     | `01 07` → 263°                |
| Время           | 6 байт BCD  | `26 06 06 17 01 48`            |

Единица скорости по JT/T808: **0.1 км/ч** (`0x0012` = 1.8 км/ч, `0x0025` = 3.7 км/ч).

#### Дополнительные TLV (после 28-байтовой базы)

Формат: `ID (1) + Длина (1) + Значение (N)`.

Присутствуют в каждом **0x0200** от этого устройства:

| TLV ID | Длина | Пример          | Примечание                              |
| ------ | ----- | --------------- | --------------------------------------- |
| 0x01   | 4     | `00 00 00 00`   | Пробег (расширение JT/T808, ID 0x01)    |
| 0x30   | 1     | `0x18` (24)     | Уровень сигнала                         |
| 0x31   | 1     | `0x00` / `0x18` | Число спутников GNSS                    |
| 0x82   | 2     | `00 7D` (125)   | Производительский; смысл не установлен  |
| 0xEA   | 2     | `00 01`         | Производительский; смысл не установлен  |
| 0x2A   | 2     | `00 00`         | Производительский; смысл не установлен  |

Только в варианте с телом 84 байта:

| TLV ID | Длина | Пример                     | Примечание                          |
| ------ | ----- | -------------------------- | ----------------------------------- |
| 0xCC   | 20    | `89997025023993210933`     | ASCII-строка, предположительно ICCID |
| 0x57   | 8     | `00 00 00 00 00 00 00 00`  | Производительский; смысл не установлен |

---

## Преобразование координат

Широта и долгота кодируются как:

```text
градусы × 1 000 000
```

Пример (подтверждено захватом):

```hex
Широта:  02 93 A7 2D  →  43231021  →  43.231021°
Долгота: 04 94 DC 47  →  76864583  →  76.864583°
```

Результат:

```text
43.231021, 76.864583
```

---

## Формат времени GPS

BCD, 6 байт.

Пример из захвата:

```hex
26 06 06 17 01 48
```

Расшифровка:

```text
2026-06-06 17:01:48
```

Формат:

```text
ГГ ММ ДД ЧЧ ММ СС
```

---

## 0x8001 — Общий ответ платформы

Используется как ACK для **0x0102**, **0x0002**, **0x0200** и других сообщений терминала.

Формат тела:

```text
Serial исходного сообщения  2 байта
ID исходного сообщения      2 байта
Результат                   1 байт
```

Коды результата (стандарт JT/T808):

| Код | Значение              |
| --- | --------------------- |
| 0   | Успех                 |
| 1   | Ошибка                |
| 2   | Ошибка сообщения      |
| 3   | Не поддерживается     |
| 4   | Подтверждение тревоги |

Все наблюдаемые ответы имели **result = 0**.

Пример (ACK на **0x0102**, serial **0x0144**):

```hex
7E 80 01 00 05 01 91 76 72 67 68 01 99 01 44 01 02 00 C1 7E
```

Пример (ACK на **0x0200**, serial **0x0147**):

```hex
7E 80 01 00 05 01 91 76 72 67 68 11 48 01 47 02 00 00 02 7E
```

Смысл:

```text
Serial сервера = 0x1148
Подтверждён serial = 0x0147
Подтверждённое сообщение = 0x0200
Результат = 0 (успех)
```

---

## Поведение устройства

После успешной аутентификации:

```text
1. Всплеск из 3× 0x0200 за ~1 секунду (serial 0x0142, 0x0143, 0x0145)
2. Один 0x0002 (пульс)
3. Следующий 0x0200 через ~6 секунд (serial 0x0147)
4. Далее 0x0200 примерно каждые ~20 секунд, пока TCP-соединение активно
```

Телеметрия продолжается, пока TCP-соединение не разорвано.

---

## Логирование в GpsTcpProxy

Прокси парсит JT/T808 и ведёт два лога.

### Основной терминал и `logs/YYYY-MM-DD.log`

Только пакеты **от устройства к серверу**. Ответы сервера (0x8001 и др.) не выводятся.

Формат:

```text
[06.06.2026 11:23:48] [9176726768] Тип пакета: телеметрия 43.231021, 76.864583 alt=842m speed=1.8km/h dir=263° time=2026-06-06 17:01:48
[06.06.2026 11:23:48] [9176726768] Тип пакета: аутентификация token=19176726768
[06.06.2026 11:23:50] [9176726768] Тип пакета: пульс
```

### Raw-лог `logs/raw_data/YYYY-MM-DD.log`

Оба направления, сырой HEX:

```text
06.06.26 11:14:48 #1 [9176726768 → 🖥️] 7E 02 00 00 34 ...
06.06.26 11:14:48 #1 [🖥️ → 9176726768] 7E 80 01 00 05 ...
```

Просмотр онлайн:

```bash
tail -f bin/Debug/net8.0/logs/raw_data/$(date +%Y-%m-%d).log
```

---

## Минимальная реализация сервера

Для работы с этим устройством на наблюдаемой платформе достаточно:

* TCP-сервер
* Парсер кадров JT808 (разделитель `0x7E`)
* Проверка XOR checksum
* Обработка аутентификации (**0x0102 → 0x8001**)
* Обработка пульса (**0x0002 → 0x8001**)
* Обработка телеметрии (**0x0200 → 0x8001**)

Не требуется в наблюдаемой сессии:

* Регистрация (**0x0100 → 0x8100**) — устройство не отправляет
* Хранение координат, Web API, карта, команды, геозоны

Для базового трекинга на `27.aika168.com:8185` достаточно **0x0102**, **0x0002**, **0x0200** и ответов **0x8001**.

---

## Не подтверждено / не проверено

Следующие пункты описаны в стандарте JT/T808 или предполагались ранее, но **не подтверждены** захватом от 06.06.2026:

| Пункт | Статус |
| ----- | ------ |
| **0x0100 Регистрация** | Не отправлялась; возможна при первой привязке или на других серверах |
| **0x8100 Ответ на регистрацию** | Не получен; формат auth-токена неизвестен |
| Auth-токен `25 7B` из 0x8100 | Не применимо — устройство шлёт terminal ID в 0x0102 |
| **Флаги статуса** `00 0C 00 02` | Значение зафиксировано; биты не расшифрованы |
| **TLV 0x82, 0xEA, 0x2A, 0x57** | Есть в каждом отчёте; производительские, смысл неизвестен |
| **TLV 0xCC** | ASCII `89997025023993210933`; предположительно ICCID |
| **XOR checksum для 0x0200** | 0x0102 и 0x0002 сходятся; для 0x0200 расхождение в 1 байт |
| **Byte stuffing** (`0x7D`) | В захвате не встречался |
| **Шифрование / субпакеты** | Биты Properties всегда 0 |
| **Команды платформы** (downlink кроме 0x8001) | Не наблюдались |
| **Тревоги** (ненулевые флаги) | Всегда `00 00 00 00` |
| **Переподключение** | Захвачена одна сессия |
| **Строгий интервал 20 с** | После всплеска: ~6 с, затем ~20 с |
